{
  "name": "gtm-arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GTM Arena — a crowdsourced leaderboard that benchmarks low-level GTM tools by category. Convex backend.",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "seed": "convex run seed:run",
    "reseed": "convex run seed:reset"
  },
  "dependencies": {
    "convex": "^1.42.0"
  }
}
